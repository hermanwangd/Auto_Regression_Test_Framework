provider_instance_version: v0.2
provider_id: dummy-rest-client
provider_type: rest_client
runtime_modes: [native]
defaults:
  timeout_seconds: 10
operations:
  http_request:
    outputs: [response.status, response.headers, response.body, response.duration_ms]
evidence:
  capture: [request, response, timing, error]
failure_mapping:
  provider_unavailable: PROVIDER_UNAVAILABLE
  timeout: PROVIDER_TIMEOUT
  operation_failed: OPERATION_FAILED
labels:
  evidence_classification: product_release_evidence_candidate
  downstream_release_evidence: true
