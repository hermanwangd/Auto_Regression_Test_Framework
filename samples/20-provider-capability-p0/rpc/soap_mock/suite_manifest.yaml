contract_version: v0.2
suite_id: SOAP-MOCK-SAMPLE-v0.2
purpose: Contract sample showing SOAP mock behavior through a WireMock-backed soap_mock provider and rest_client stimulus.
profile: local_soap_mock
selection:
  mode: suite
  suite: provider-capability-soap-mock
tests:
  - test_case.yaml
artifact_roots:
  provider_instances: provider_instances/
  env_profiles: env_profiles/
  execution_profiles: execution_profiles/
  environment_bindings: environment_bindings/
  expected_results: expected_results/
  fixtures: fixtures/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
