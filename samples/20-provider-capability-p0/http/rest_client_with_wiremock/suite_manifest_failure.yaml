contract_version: v0.2
suite_id: WIREMOCK-HTTP-REQUEST-FAILURE-SAMPLE-v0.2
purpose: Negative executable sample proving assertion failure evidence for WireMock plus rest_client.
profile: local_wiremock_http
selection:
  mode: suite
  suite: provider-capability-wiremock-http-request-failure
tests:
  - test_case_failure.yaml
artifact_roots:
  provider_instances: provider_instances/
  expected_results: expected_results/
  fixtures: fixtures/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
