provider_instance_version: v0.2
provider_id: wiremock-payment-api
provider_type: wiremock_http_mock
runtime_modes: [mock]
defaults:
  startup_timeout: PT10S
  reset_before_load: true
  request_journal_scope: per_run
operations:
  load_stubs:
    outputs: [base_url, loaded_stub_count, injected_stub, server_log]
  send_http_request:
    outputs: [base_url, http_status, response_body, matched_count, request_journal, server_log]
  reset_mock:
    outputs: [cleanup_log, server_log]
evidence:
  capture: [base_url, request_journal, server_log, injected_stub, assertion_result, failure_detail]
failure_mapping:
  provider_unavailable: PROVIDER_UNAVAILABLE
  stub_missing: PROVIDER_STUB_MISSING
  request_missing: ASSERTION_FAILED
  body_mismatch: ASSERTION_FAILED
labels:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
