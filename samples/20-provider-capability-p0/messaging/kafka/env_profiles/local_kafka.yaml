env_profile_id: local_kafka
execution_mode: local
providers:
  order-events:
    runtime_mode: mock
    bindings:
      bootstrap_servers: approved_local_kafka_ref
      topic: orders.created
      consumer_group: artf-local-kafka
      timeout: PT5S
      poll_interval: PT0.05S
