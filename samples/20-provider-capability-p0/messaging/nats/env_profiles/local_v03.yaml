profile_id: local_v03
execution_mode: local
isolation: per_run
evidence_classification: framework_verification_only

targets:
  event_bus:
    runtime_mode: mock
    bindings:
      connection.local_ref: approved_local_nats_ref
      subject: orders.ready.v03
      timeout: PT2S
      poll_interval: PT0.01S
