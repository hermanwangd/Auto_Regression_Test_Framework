profile_id: local_v03
execution_mode: local
isolation: per_run
evidence_classification: framework_verification_only

targets:
  order_db:
    runtime_mode: ephemeral
    bindings:
      dialect: oracle
      connection.local_ref: approved_local_h2_oracle
      strict_params: true
