contract_version: v0.2
suite_id: SUITE-MODE-DUMMY-REST-v0.2
purpose: v0.2.6 suite-mode demo proving a custom REST target can execute through the built-in rest_client provider.
profile: local_dummy
selection:
  mode: suite
  suite: suite-mode-dummy-rest
tests:
  - test_case.yaml
artifact_roots:
  provider_instances: provider_instances/
  env_profiles: env_profiles/
  expected_results: expected_results/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
