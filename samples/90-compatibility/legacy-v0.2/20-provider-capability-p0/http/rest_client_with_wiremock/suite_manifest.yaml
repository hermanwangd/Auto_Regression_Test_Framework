contract_version: v0.2
suite_id: WIREMOCK-HTTP-REQUEST-SAMPLE-v0.2
purpose: Contract sample showing WireMock as a mocked dependency and rest_client as the HTTP request provider.
profile: local_wiremock_http
selection:
  mode: suite
  suite: provider-capability-wiremock-http-request
tests:
  - test_case.yaml
artifact_roots:
  provider_instances: provider_instances/
  env_profiles: env_profiles/
  expected_results: expected_results/
  fixtures: fixtures/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
