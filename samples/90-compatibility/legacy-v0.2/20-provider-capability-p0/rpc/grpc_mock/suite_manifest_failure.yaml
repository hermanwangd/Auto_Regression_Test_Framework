contract_version: v0.2
suite_id: GRPC-MOCK-SAMPLE-FAILURE-v0.2
purpose: Failure-path unary gRPC mock sample proving assertion evidence is produced when expected request count is not met.
profile: local_grpc_mock
selection:
  mode: suite
  suite: provider-capability-grpc-mock-failure
tests:
  - test_case_failure.yaml
artifact_roots:
  provider_instances: provider_instances/
  env_profiles: env_profiles/
  fixtures: fixtures/
  proto: proto/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
