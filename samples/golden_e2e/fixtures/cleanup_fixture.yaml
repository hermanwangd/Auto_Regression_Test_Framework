fixture_version: v0.2
fixture_id: golden-e2e-cleanup
type: sample_cleanup
target_provider_id: sample-fake-runtime
operation: cleanup_fixture
cleanup_policy: always
inputs:
  workspace_ref: local://golden-e2e-workspace
expected_outputs:
  - cleanup_log
