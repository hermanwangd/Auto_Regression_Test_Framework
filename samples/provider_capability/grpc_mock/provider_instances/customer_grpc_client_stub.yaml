provider_instance_version: v0.2
provider_id: customer-grpc-client-stub-contract-sample
provider_type: grpc_client
runtime_modes: [stub]
defaults:
  timeout_seconds: 10
operations:
  unary_call:
    outputs: [response.message, response.status, response.metadata, response.duration_ms]
evidence:
  capture: [request, response, status, metadata, timing, error]
failure_mapping:
  provider_unavailable: PROVIDER_UNAVAILABLE
  timeout: PROVIDER_TIMEOUT
  operation_failed: OPERATION_FAILED
labels:
  sample_scope: usage_kit_runtime_mode_sample
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
