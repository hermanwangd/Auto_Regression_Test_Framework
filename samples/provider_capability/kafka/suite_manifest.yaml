contract_version: v0.2
suite_id: KAFKA-CAPABILITY-v0.2
purpose: Framework-owned Kafka client provider capability verification sample.
profile: local_kafka
selection:
  mode: suite
  suite: provider-capability-kafka
tests:
  - test_case.yaml
artifact_roots:
  provider_instances: provider_instances/
  env_profiles: env_profiles/
  expected_results: expected_results/
  fixtures: fixtures/
evidence_policy:
  evidence_classification: framework_provider_capability_only
  downstream_release_evidence: false
