profile_id: local_v03
execution_mode: local
targets:
  order_db:
    runtime_mode: native
    bindings:
      dialect: oracle
      connection.secret_ref: jdbc:oracle:thin:user/password@localhost:1521/XEPDB1
