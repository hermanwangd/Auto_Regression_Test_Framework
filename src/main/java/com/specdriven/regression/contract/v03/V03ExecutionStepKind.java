package com.specdriven.regression.contract.v03;

/** The executable semantic kind of a compiled v0.3 step. */
public enum V03ExecutionStepKind {
    PROVIDER_OPERATION,
    PROVIDER_CHECK,
    ASSERTION
}
