# Runner Plugin Contract v0.2

Status: compatibility alias.

`runner_plugin_contract.md` is retained only for older contract-test and
generated-artifact references. The canonical v0.2 public runtime extension
surface is `provider_plugin_contract.md` plus Provider Contract / Provider
Instance configuration. New user-facing documentation should use Provider
naming, not runner or adapter naming.
